using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TurnBarAutoFlip : MonoBehaviour
{
    public List<TurnBarSlider> Allies;
    public List<TurnBarSlider> Enemies;

    // Update is called once per frame
    void Update()
    {
        foreach (var item in Allies)
        {
            
        }
    }
}
